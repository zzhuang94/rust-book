[package]
name = "lang-functions-closures"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/lang/functions-closures.md
# 运行（在 code/ 下）：cargo run -p lang-functions-closures

[dependencies]
labkit = { workspace = true }
