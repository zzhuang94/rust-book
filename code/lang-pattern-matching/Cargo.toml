[package]
name = "lang-pattern-matching"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/lang/pattern-matching.md
# 运行（在 code/ 下）：cargo run -p lang-pattern-matching

[dependencies]
labkit = { workspace = true }
