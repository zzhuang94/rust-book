[package]
name = "lang-error-handling"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/lang/error-handling.md
# 运行（在 code/ 下）：cargo run -p lang-error-handling

[dependencies]
labkit = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
