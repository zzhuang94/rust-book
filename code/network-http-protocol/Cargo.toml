[package]
name = "network-http-protocol"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/network/http-protocol.md
# 运行（在 code/ 下）：cargo run -p network-http-protocol

[dependencies]
tokio = { workspace = true }
anyhow = { workspace = true }
labkit = { workspace = true }
