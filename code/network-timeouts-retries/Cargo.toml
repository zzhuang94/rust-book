[package]
name = "network-timeouts-retries"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/network/timeouts-retries.md
# 运行（在 code/ 下）：cargo run -p network-timeouts-retries

[dependencies]
tokio = { workspace = true }
anyhow = { workspace = true }
labkit = { workspace = true }
