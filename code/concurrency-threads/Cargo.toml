[package]
name = "concurrency-threads"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/concurrency/threads.md
# 运行（在 code/ 下）：cargo run -p concurrency-threads

[dependencies]
labkit = { workspace = true }
