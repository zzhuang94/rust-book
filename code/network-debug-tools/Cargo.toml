[package]
name = "network-debug-tools"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/network/debug-tools.md
# 运行（在 code/ 下）：cargo run -p network-debug-tools

[dependencies]
tokio = { workspace = true }
anyhow = { workspace = true }
labkit = { workspace = true }
