[package]
name = "os-sync-primitives"
version = "0.1.0"
edition = "2021"

# 配套文档：docs/os/sync-primitives.md
# 运行（在 code/ 下）：cargo run -p os-sync-primitives
# 全部标准库，不需要 tokio。

[dependencies]
labkit = { workspace = true }
